{%- comment -%}
  Feed Landing — reusable programmatic-SEO / GEO-AEO landing section for Meru Trophy.
  One section type powers all /pages/* "feed" landing pages (product, combo, pricing,
  process, informational, comparison and occasion archetypes). All content is supplied
  per-page through the template JSON settings + blocks, so a new page = a new template file.

  Blocks: quick_answer, rich_text, feature, stat, price_row, step, faq, cta_band, related_collection
{%- endcomment -%}

{%- liquid
  assign wa_number = '917892207252'
  assign phone_display = '+91 78922 07252'
  assign primary_url = section.settings.primary_cta_url
  if primary_url == blank
    assign wa_text = section.settings.whatsapp_prefill | default: 'Hi Meru Trophy, I have an enquiry from your website.'
    assign primary_url = 'https://wa.me/' | append: wa_number | append: '?text=' | append: wa_text | url_encode
    comment
    url_encode on the whole string breaks the URL; build manually below instead
    endcomment
    assign primary_url = 'https://wa.me/' | append: wa_number
  endif
-%}

{%- style -%}
  .feed-ld-{{ section.id }} { --feed-gold: #d4a017; --feed-gold-2: #f5b400; }
  .feed-ld-{{ section.id }} .page-width { max-width: 104rem; }
  .feed-ld-{{ section.id }} .feed-pad {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }
  @media screen and (min-width: 750px) {
    .feed-ld-{{ section.id }} .feed-pad {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
  .feed-breadcrumb { font-size: 1.3rem; color: rgba(var(--color-foreground), 0.6); margin-bottom: 1.6rem; }
  .feed-breadcrumb a { color: inherit; text-decoration: none; }
  .feed-breadcrumb a:hover { text-decoration: underline; }
  .feed-hero { max-width: 80rem; }
  .feed-eyebrow {
    display: inline-block; font-size: 1.2rem; letter-spacing: 0.28rem; text-transform: uppercase;
    color: var(--feed-gold); margin-bottom: 1.2rem; font-weight: 600;
  }
  .feed-hero h1 { margin: 0 0 1.4rem; line-height: 1.12; font-size: 3.6rem; }
  @media screen and (min-width: 750px) { .feed-hero h1 { font-size: 4.6rem; } }
  .feed-hero h1 em {
    font-style: normal; background: linear-gradient(135deg, var(--feed-gold), var(--feed-gold-2));
    -webkit-background-clip: text; background-clip: text; color: transparent;
  }
  .feed-sub { font-size: 1.7rem; line-height: 1.6; color: rgba(var(--color-foreground), 0.82); margin: 0 0 2.4rem; }
  .feed-cta-row { display: flex; flex-wrap: wrap; gap: 1.2rem; margin-bottom: 1.2rem; }
  .feed-btn {
    display: inline-flex; align-items: center; gap: 0.8rem; padding: 1.2rem 2.4rem; border-radius: 0.6rem;
    font-size: 1.5rem; font-weight: 600; text-decoration: none; cursor: pointer; border: 1px solid transparent;
  }
  .feed-btn--primary { background: #25D366; color: #fff; }
  .feed-btn--secondary { background: transparent; color: rgb(var(--color-foreground)); border-color: rgba(var(--color-foreground), 0.28); }
  .feed-btn:hover { transform: translateY(-1px); }
  .feed-answer {
    background: rgba(var(--color-foreground), 0.04); border-left: 4px solid var(--feed-gold);
    border-radius: 0.8rem; padding: 2rem 2.4rem; margin: 3.2rem 0; font-size: 1.6rem; line-height: 1.65;
  }
  .feed-answer__label { font-size: 1.1rem; letter-spacing: 0.18rem; text-transform: uppercase; color: var(--feed-gold); font-weight: 700; margin-bottom: 0.8rem; }
  .feed-answer p:last-child { margin-bottom: 0; }
  .feed-body { font-size: 1.6rem; line-height: 1.7; }
  .feed-body h2 { font-size: 2.6rem; margin: 3.6rem 0 1.2rem; }
  .feed-body h3 { font-size: 2rem; margin: 2.8rem 0 1rem; }
  .feed-body ul, .feed-body ol { padding-left: 2rem; }
  .feed-body li { margin-bottom: 0.6rem; }
  .feed-stats { display: flex; flex-wrap: wrap; gap: 3.2rem; margin: 3.2rem 0; }
  .feed-stat__num { font-size: 3.2rem; font-weight: 700; line-height: 1; background: linear-gradient(135deg, var(--feed-gold), var(--feed-gold-2)); -webkit-background-clip: text; background-clip: text; color: transparent; }
  .feed-stat__label { font-size: 1.3rem; color: rgba(var(--color-foreground), 0.7); margin-top: 0.6rem; }
  .feed-features { display: grid; grid-template-columns: 1fr; gap: 1.6rem; margin: 2.4rem 0; }
  @media screen and (min-width: 750px) { .feed-features { grid-template-columns: 1fr 1fr; } }
  .feed-feature { background: rgba(var(--color-foreground), 0.035); border-radius: 0.8rem; padding: 1.8rem 2rem; }
  .feed-feature__title { font-size: 1.7rem; font-weight: 600; margin: 0 0 0.6rem; }
  .feed-feature__text { font-size: 1.45rem; line-height: 1.55; color: rgba(var(--color-foreground), 0.78); margin: 0; }
  .feed-steps { counter-reset: feedstep; margin: 2.4rem 0; display: grid; gap: 1.2rem; }
  .feed-step { position: relative; padding-left: 4.6rem; }
  .feed-step::before { counter-increment: feedstep; content: counter(feedstep); position: absolute; left: 0; top: 0; width: 3.2rem; height: 3.2rem; border-radius: 50%; background: linear-gradient(135deg, var(--feed-gold), var(--feed-gold-2)); color: #fff; font-weight: 700; display: flex; align-items: center; justify-content: center; }
  .feed-step__title { font-size: 1.7rem; font-weight: 600; margin: 0.3rem 0 0.4rem; }
  .feed-step__text { font-size: 1.45rem; line-height: 1.55; color: rgba(var(--color-foreground), 0.78); margin: 0; }
  .feed-table-wrap { overflow-x: auto; margin: 2.4rem 0; }
  .feed-table { width: 100%; border-collapse: collapse; font-size: 1.5rem; }
  .feed-table th, .feed-table td { text-align: left; padding: 1.2rem 1.4rem; border-bottom: 1px solid rgba(var(--color-foreground), 0.12); }
  .feed-table th { font-size: 1.2rem; letter-spacing: 0.06rem; text-transform: uppercase; color: rgba(var(--color-foreground), 0.6); }
  .feed-table td:last-child { font-weight: 700; white-space: nowrap; }
  .feed-price-caption { font-size: 1.3rem; color: rgba(var(--color-foreground), 0.6); margin-top: 0.8rem; }
  .feed-faq { margin: 2.4rem 0; }
  .feed-faq details { border-bottom: 1px solid rgba(var(--color-foreground), 0.14); padding: 1.4rem 0; }
  .feed-faq summary { font-size: 1.7rem; font-weight: 600; cursor: pointer; list-style: none; display: flex; justify-content: space-between; gap: 1.2rem; }
  .feed-faq summary::-webkit-details-marker { display: none; }
  .feed-faq summary::after { content: '+'; color: var(--feed-gold); font-size: 2.2rem; line-height: 1; }
  .feed-faq details[open] summary::after { content: '–'; }
  .feed-faq__a { font-size: 1.5rem; line-height: 1.65; color: rgba(var(--color-foreground), 0.82); margin-top: 1rem; }
  .feed-ctaband { background: linear-gradient(135deg, rgba(212,160,23,0.12), rgba(245,180,0,0.06)); border: 1px solid rgba(212,160,23,0.3); border-radius: 1.2rem; padding: 3.2rem; text-align: center; margin: 4rem 0 0; }
  .feed-ctaband h2 { margin: 0 0 1rem; font-size: 2.6rem; }
  .feed-ctaband p { font-size: 1.6rem; color: rgba(var(--color-foreground), 0.8); margin: 0 auto 2rem; max-width: 60rem; }
  .feed-related { margin: 4rem 0 0; }
  .feed-related h2 { font-size: 2.6rem; margin: 0 0 1.6rem; }
  .feed-related__grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.6rem; }
  @media screen and (min-width: 750px) { .feed-related__grid { grid-template-columns: repeat(4, 1fr); } }
  .feed-blocktitle { font-size: 2.6rem; margin: 3.6rem 0 1.2rem; }
{%- endstyle -%}

<div class="color-{{ section.settings.color_scheme }} gradient feed-ld-{{ section.id }}">
  <div class="page-width feed-pad">

    {%- if section.settings.show_breadcrumb -%}
      <nav class="feed-breadcrumb" aria-label="Breadcrumb">
        <a href="{{ routes.root_url }}">Home</a> &rsaquo;
        <a href="/pages/feeds">Feeds</a> &rsaquo;
        <span>{{ section.settings.breadcrumb_label | default: page.title }}</span>
      </nav>
    {%- endif -%}

    <header class="feed-hero">
      {%- if section.settings.eyebrow != blank -%}<span class="feed-eyebrow">{{ section.settings.eyebrow }}</span>{%- endif -%}
      {%- if section.settings.heading != blank -%}{{ section.settings.heading }}{%- else -%}<h1>{{ page.title }}</h1>{%- endif -%}
      {%- if section.settings.subheading != blank -%}<p class="feed-sub">{{ section.settings.subheading }}</p>{%- endif -%}
      <div class="feed-cta-row">
        <a class="feed-btn feed-btn--primary" href="{{ primary_url }}" target="_blank" rel="noopener">
          <svg viewBox="0 0 32 32" width="18" height="18" fill="currentColor" aria-hidden="true"><path d="M16 3C9.373 3 4 8.373 4 15c0 2.385.668 4.61 1.832 6.502L4 29l7.701-1.814A11.94 11.94 0 0016 27c6.627 0 12-5.373 12-12S22.627 3 16 3zm0 22a9.95 9.95 0 01-5.078-1.393l-.364-.217-4.573 1.077 1.099-4.461-.237-.376A9.953 9.953 0 016 15c0-5.514 4.486-10 10-10s10 4.486 10 10-4.486 10-10 10z"/></svg>
          {{ section.settings.primary_cta_label | default: 'Get a quote on WhatsApp' }}
        </a>
        {%- if section.settings.secondary_cta_label != blank -%}
          <a class="feed-btn feed-btn--secondary" href="{{ section.settings.secondary_cta_url | default: '/collections/all' }}">{{ section.settings.secondary_cta_label }}</a>
        {%- endif -%}
      </div>
    </header>

    {%- for block in section.blocks -%}
      {%- case block.type -%}

        {%- when 'quick_answer' -%}
          <div class="feed-answer" {{ block.shopify_attributes }}>
            <div class="feed-answer__label">{{ block.settings.label | default: 'Quick answer' }}</div>
            {{ block.settings.text }}
          </div>

        {%- when 'rich_text' -%}
          <div class="feed-body" {{ block.shopify_attributes }}>
            {%- if block.settings.heading != blank -%}<h2 class="feed-blocktitle">{{ block.settings.heading }}</h2>{%- endif -%}
            {{ block.settings.text }}
          </div>

        {%- when 'stat' -%}
          {%- comment -%} stats are grouped via wrapper rendered below {%- endcomment -%}

        {%- when 'feature' -%}
        {%- when 'price_row' -%}
        {%- when 'step' -%}
        {%- when 'faq' -%}

        {%- when 'cta_band' -%}
          <div class="feed-ctaband" {{ block.shopify_attributes }}>
            <h2>{{ block.settings.heading | default: 'Ready to order?' }}</h2>
            {%- if block.settings.text != blank -%}<p>{{ block.settings.text }}</p>{%- endif -%}
            <a class="feed-btn feed-btn--primary" href="{{ primary_url }}" target="_blank" rel="noopener" style="margin:0 auto;">
              {{ block.settings.button_label | default: 'Message us on WhatsApp' }}
            </a>
          </div>

        {%- when 'related_collection' -%}
          {%- assign coll = collections[block.settings.collection] -%}
          {%- if coll != blank and coll.products_count > 0 -%}
            <div class="feed-related" {{ block.shopify_attributes }}>
              <h2>{{ block.settings.heading | default: 'Popular in this range' }}</h2>
              <div class="feed-related__grid">
                {%- for product in coll.products limit: 4 -%}
                  {% render 'card-product', card_product: product, show_vendor: false, lazy_load: true %}
                {%- endfor -%}
              </div>
            </div>
          {%- endif -%}

      {%- endcase -%}
    {%- endfor -%}

    {%- comment -%} Grouped renders: stats, features, price table, steps, FAQ {%- endcomment -%}

    {%- assign stat_blocks = section.blocks | where: 'type', 'stat' -%}
    {%- if stat_blocks.size > 0 -%}
      <div class="feed-stats">
        {%- for block in stat_blocks -%}
          <div {{ block.shopify_attributes }}>
            <div class="feed-stat__num">{{ block.settings.number }}</div>
            <div class="feed-stat__label">{{ block.settings.label }}</div>
          </div>
        {%- endfor -%}
      </div>
    {%- endif -%}

    {%- assign feature_blocks = section.blocks | where: 'type', 'feature' -%}
    {%- if feature_blocks.size > 0 -%}
      {%- if section.settings.features_heading != blank -%}<h2 class="feed-blocktitle">{{ section.settings.features_heading }}</h2>{%- endif -%}
      <div class="feed-features">
        {%- for block in feature_blocks -%}
          <div class="feed-feature" {{ block.shopify_attributes }}>
            <p class="feed-feature__title">{{ block.settings.title }}</p>
            <p class="feed-feature__text">{{ block.settings.text }}</p>
          </div>
        {%- endfor -%}
      </div>
    {%- endif -%}

    {%- assign price_blocks = section.blocks | where: 'type', 'price_row' -%}
    {%- if price_blocks.size > 0 -%}
      {%- if section.settings.price_heading != blank -%}<h2 class="feed-blocktitle">{{ section.settings.price_heading }}</h2>{%- endif -%}
      <div class="feed-table-wrap">
        <table class="feed-table">
          <thead><tr>
            <th>{{ section.settings.price_col1 | default: 'Item' }}</th>
            <th>{{ section.settings.price_col2 | default: 'Details' }}</th>
            <th>{{ section.settings.price_col3 | default: 'Price (from)' }}</th>
          </tr></thead>
          <tbody>
            {%- for block in price_blocks -%}
              <tr {{ block.shopify_attributes }}>
                <td>{{ block.settings.item }}</td>
                <td>{{ block.settings.spec }}</td>
                <td>{{ block.settings.price }}</td>
              </tr>
            {%- endfor -%}
          </tbody>
        </table>
      </div>
      {%- if section.settings.price_caption != blank -%}<p class="feed-price-caption">{{ section.settings.price_caption }}</p>{%- endif -%}
    {%- endif -%}

    {%- assign step_blocks = section.blocks | where: 'type', 'step' -%}
    {%- if step_blocks.size > 0 -%}
      {%- if section.settings.steps_heading != blank -%}<h2 class="feed-blocktitle">{{ section.settings.steps_heading }}</h2>{%- endif -%}
      <div class="feed-steps">
        {%- for block in step_blocks -%}
          <div class="feed-step" {{ block.shopify_attributes }}>
            <p class="feed-step__title">{{ block.settings.title }}</p>
            <p class="feed-step__text">{{ block.settings.text }}</p>
          </div>
        {%- endfor -%}
      </div>
    {%- endif -%}

    {%- assign faq_blocks = section.blocks | where: 'type', 'faq' -%}
    {%- if faq_blocks.size > 0 -%}
      <h2 class="feed-blocktitle">{{ section.settings.faq_heading | default: 'Frequently asked questions' }}</h2>
      <div class="feed-faq">
        {%- for block in faq_blocks -%}
          <details {{ block.shopify_attributes }}{% if forloop.first %} open{% endif %}>
            <summary>{{ block.settings.question }}</summary>
            <div class="feed-faq__a">{{ block.settings.answer }}</div>
          </details>
        {%- endfor -%}
      </div>
    {%- endif -%}

  </div>
</div>

{%- comment -%} ---------- JSON-LD structured data for SEO + AI citation ---------- {%- endcomment -%}
{%- capture page_url -%}{{ shop.url }}{{ page.url }}{%- endcapture -%}

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    { "@type": "ListItem", "position": 1, "name": "Home", "item": "{{ shop.url }}" },
    { "@type": "ListItem", "position": 2, "name": "Feeds", "item": "{{ shop.url }}/pages/feeds" },
    { "@type": "ListItem", "position": 3, "name": {{ section.settings.breadcrumb_label | default: page.title | json }}, "item": "{{ page_url }}" }
  ]
}
</script>

{%- if faq_blocks.size > 0 -%}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {%- for block in faq_blocks -%}
    {
      "@type": "Question",
      "name": {{ block.settings.question | json }},
      "acceptedAnswer": { "@type": "Answer", "text": {{ block.settings.answer | strip_html | json }} }
    }{% unless forloop.last %},{% endunless %}
    {%- endfor -%}
  ]
}
</script>
{%- endif -%}

{%- if section.settings.schema_type == 'Product' -%}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Product",
  "name": {{ section.settings.schema_name | default: page.title | json }},
  "description": {{ page.metafields.global.description_tag | default: section.settings.subheading | strip_html | json }},
  "brand": { "@type": "Brand", "name": "Meru Trophy" },
  "url": "{{ page_url }}"
  {%- if section.settings.schema_price_low != blank -%},
  "offers": {
    "@type": "AggregateOffer",
    "priceCurrency": "INR",
    "lowPrice": "{{ section.settings.schema_price_low }}"{% if section.settings.schema_price_high != blank %},
    "highPrice": "{{ section.settings.schema_price_high }}"{% endif %},
    "seller": { "@type": "Organization", "name": "Meru Trophy" }
  }
  {%- endif -%}
}
</script>
{%- elsif section.settings.schema_type == 'Service' -%}
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Service",
  "serviceType": {{ section.settings.schema_name | default: page.title | json }},
  "provider": { "@type": "Organization", "name": "Meru Trophy", "url": "{{ shop.url }}", "telephone": "+917892207252" },
  "areaServed": "IN",
  "url": "{{ page_url }}"
}
</script>
{%- endif -%}

{% schema %}
{
  "name": "Feed Landing",
  "tag": "section",
  "class": "feed-landing-section",
  "settings": [
    { "type": "header", "content": "Hero" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow" },
    { "type": "richtext", "id": "heading", "label": "Heading (H1)", "info": "Use <h1>…</h1>. Wrap a phrase in <em></em> for the gold gradient." },
    { "type": "textarea", "id": "subheading", "label": "Subheading" },
    { "type": "text", "id": "primary_cta_label", "label": "Primary CTA label", "default": "Get a quote on WhatsApp" },
    { "type": "url", "id": "primary_cta_url", "label": "Primary CTA URL", "info": "Leave blank to default to WhatsApp." },
    { "type": "text", "id": "secondary_cta_label", "label": "Secondary CTA label", "default": "Browse the catalogue" },
    { "type": "url", "id": "secondary_cta_url", "label": "Secondary CTA URL" },
    { "type": "header", "content": "Breadcrumb" },
    { "type": "checkbox", "id": "show_breadcrumb", "label": "Show breadcrumb", "default": true },
    { "type": "text", "id": "breadcrumb_label", "label": "Breadcrumb / current page label" },
    { "type": "header", "content": "Section block headings" },
    { "type": "text", "id": "features_heading", "label": "Features heading" },
    { "type": "text", "id": "steps_heading", "label": "Steps heading" },
    { "type": "header", "content": "Price table" },
    { "type": "text", "id": "price_heading", "label": "Price table heading" },
    { "type": "text", "id": "price_col1", "label": "Column 1 label", "default": "Item" },
    { "type": "text", "id": "price_col2", "label": "Column 2 label", "default": "Details" },
    { "type": "text", "id": "price_col3", "label": "Column 3 label", "default": "Price (from)" },
    { "type": "text", "id": "price_caption", "label": "Price caption" },
    { "type": "text", "id": "faq_heading", "label": "FAQ heading", "default": "Frequently asked questions" },
    { "type": "header", "content": "Structured data (JSON-LD)" },
    { "type": "select", "id": "schema_type", "label": "Schema type", "options": [ {"value":"none","label":"None"}, {"value":"Product","label":"Product"}, {"value":"Service","label":"Service"} ], "default": "Product" },
    { "type": "text", "id": "schema_name", "label": "Schema name" },
    { "type": "text", "id": "schema_price_low", "label": "Schema low price (INR, number only)" },
    { "type": "text", "id": "schema_price_high", "label": "Schema high price (INR, number only)" },
    { "type": "header", "content": "Layout" },
    { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-1" },
    { "type": "range", "id": "padding_top", "min": 0, "max": 100, "step": 4, "unit": "px", "label": "Top padding", "default": 48 },
    { "type": "range", "id": "padding_bottom", "min": 0, "max": 100, "step": 4, "unit": "px", "label": "Bottom padding", "default": 64 }
  ],
  "blocks": [
    { "type": "quick_answer", "name": "Quick answer (AEO box)", "settings": [
      { "type": "text", "id": "label", "label": "Label", "default": "Quick answer" },
      { "type": "richtext", "id": "text", "label": "Answer", "info": "1–3 sentence direct answer for AI engines & featured snippets." }
    ]},
    { "type": "rich_text", "name": "Rich text", "settings": [
      { "type": "text", "id": "heading", "label": "Heading (H2)" },
      { "type": "richtext", "id": "text", "label": "Body" }
    ]},
    { "type": "feature", "name": "Feature", "settings": [
      { "type": "text", "id": "title", "label": "Title" },
      { "type": "textarea", "id": "text", "label": "Text" }
    ]},
    { "type": "stat", "name": "Stat", "settings": [
      { "type": "text", "id": "number", "label": "Number", "default": "20+" },
      { "type": "text", "id": "label", "label": "Label", "default": "Years" }
    ]},
    { "type": "price_row", "name": "Price row", "settings": [
      { "type": "text", "id": "item", "label": "Item" },
      { "type": "text", "id": "spec", "label": "Details" },
      { "type": "text", "id": "price", "label": "Price" }
    ]},
    { "type": "step", "name": "Step", "settings": [
      { "type": "text", "id": "title", "label": "Title" },
      { "type": "textarea", "id": "text", "label": "Text" }
    ]},
    { "type": "faq", "name": "FAQ", "settings": [
      { "type": "text", "id": "question", "label": "Question" },
      { "type": "richtext", "id": "answer", "label": "Answer" }
    ]},
    { "type": "cta_band", "name": "CTA band", "settings": [
      { "type": "text", "id": "heading", "label": "Heading", "default": "Ready to order?" },
      { "type": "textarea", "id": "text", "label": "Text" },
      { "type": "text", "id": "button_label", "label": "Button label", "default": "Message us on WhatsApp" }
    ]},
    { "type": "related_collection", "name": "Related collection", "settings": [
      { "type": "text", "id": "heading", "label": "Heading", "default": "Popular in this range" },
      { "type": "collection", "id": "collection", "label": "Collection" }
    ]}
  ],
  "presets": [ { "name": "Feed Landing", "category": "Meru Trophy" } ]
}
{% endschema %}
